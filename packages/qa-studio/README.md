# @spike-land-ai/qa-studio

QA Studio exposes the spike.land browser automation tools as a local MCP server.

## Usage

```bash
npx @spike-land-ai/qa-studio --http --visible
```

The server starts on `http://127.0.0.1:3100/mcp` by default.
