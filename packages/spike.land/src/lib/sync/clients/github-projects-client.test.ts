/**
 * Tests for GitHubProjectsClient
 *
 * Tests have been split into focused suites:
 * - github-projects-client.query.test.ts    — query/read operations
 * - github-projects-client.mutation.test.ts — write/mutation operations
 * - github-projects-client.error.test.ts   — error handling & utility functions
 */

import { describe, it } from "vitest";

describe("GitHubProjectsClient (split)", () => {
  it("tests are in focused suite files — see query/mutation/error test files", () => {
    // This file is intentionally minimal. All tests live in the split files.
  });
});
