/**
 * Tests for ToolRegistry
 *
 * Tests have been split into focused suites:
 * - tool-registry.register.test.ts — constructor and registration operations
 * - tool-registry.lookup.test.ts  — search, lookup, and query operations
 * - tool-registry.filter.test.ts  — enable/disable/category filtering operations
 */

import { describe, it } from "vitest";

describe("ToolRegistry (split)", () => {
  it("tests are in focused suite files — see register/lookup/filter test files", () => {
    // This file is intentionally minimal. All tests live in the split files.
  });
});
