/**
 * Next.js src/instrumentation hook
 *
 * Note: The main instrumentation.ts at the project root handles
 * server-side error capture and shutdown. This file is kept for
 * any future src-level instrumentation needs.
 */

export async function register() {
  // Server-side instrumentation is handled by the root instrumentation.ts
}
